# PROJECT_NOTE — ピタゴラン試作

作成: 2026-08-09

ピタゴラン（コースを組み替えて玉の挙動を変えるスマホゲーム）が作れるか、の実地確認。
**作れる。ただし面白さの9割は「物理エンジンをそのまま使うか、どこで嘘をつくか」の調整にある。**
以下、詰まった所と答えだけ残す。

---

## 1. ドミノは物理エンジンだけでは倒れない

一番時間を食った。matter-js に 16×68 の箱を並べて玉をぶつけると、
**先頭が 38° 傾いて隣に寄りかかったまま止まる**。連鎖が伝わらない。

試した順と効果:

| 手 | 結果 |
|---|---|
| 間隔を変える（30〜58px） | どの間隔でも止まる。無効 |
| 細く・軽く（13×72、密度 0.0022→0.0009） | 少し伝わるが 3本目で失速 |
| 背を伸ばす（→13×84）＋ソルバ反復増（position 12 / velocity 10） | 単独チェーンは通る。ステージでは失速 |
| **傾いたら最後まで倒れる補助を足す** | **解決** |

補助の中身は `game.js` の `assistDominoes()`。倒れかけ（0.2〜1.45rad）で
回転が鈍っているドミノに毎フレーム角速度を 0.013 足すだけ。

> 学び: **寄りかかって静止するのは剛体ソルバとして正しい挙動**で、パラメータでは消えない。
> ドミノが看板パーツなら「倒れ切ることは保証する」と決めて、そこだけ嘘をつくのが正解。

初速を人工的に与える実験（`anglePrev` を 0.10 ずらす）で
**「エネルギー不足」か「静的につり合っている」かを切り分けられた**のが効いた。
キック有りで通る＝エネルギー問題、と分かった時点で方針が決まった。

## 2. ゴールは「上からしか入らない入れ物」にする

カゴを sensor だけで作ると、床を転がってきた玉が横から入ってクリアになる。
左右の縁と底を静的ボディで組んで、中に sensor を置く形にした。
これでステージ2の「玉は届かない／てっきゅうだけが入る」が成立する。

## 3. 落下地点のブレは、パーツではなくステージで吸収する

ドミノに弾かれたてっきゅうの着地点は、叩かれ方で 636〜937px とばらついた。
密度をいじっても収束しない（重くすると弾みが減る代わりに転がりが伸びる）。

**カゴの上に「じょうご」を固定物として置いた**ら、7レイアウト中6つがクリアに。
ステージ3も同様に受け皿を足して、8/81 → 実用的な難度になった。

> 学び: 物理のばらつきはパーツ側の数値で潰さない。**ステージの形で受け止める**。
> ピタゴラン系は「多少ズレても拾ってくれる」形が入っているはず。

## 4. rAF が止まる環境でも詰められる仕組みを先に作った

ブラウザペインが非表示だと requestAnimationFrame が回らず、スクショも撮れない。
そこで2つ用意した。これが無かったら調整は不可能だった。

- `vite.config.js` の `/__shot`: ページから canvas を POST → `screenshots/*.png` に保存
- `window.__pita`: `load / clear / put / play / step / draw` のコマ送りAPI

おかげで「81通りの配置を総当たりして何通りクリアするか」を数秒で測れた。
**パズルの解の存在確認と難度測定を目視でなく数で回せる**のは、この手のゲームでは強い。

罠: `play()` は `state.running` が true だと早期 return する。
総当たりの前に `back()` を挟まないと、全試行が同じ結果を返して気付きにくい。

## 5. 現状の正解例（回帰確認用）

盤面は 540×810（縦長）。`__pita.load(i)` の i は 0 始まり。

```js
0: [['rail',200,250,0.25],['rail',300,570,0.35]]                    // 1 道をつくる   6.6s
1: [270,325,405,440].map(x=>['domino',x,279,0])                     // 2 ドミノ       5.5s
2: [['jump',235,300,0.3]]                                           // 3 風とジャンプ 3.7s
3: [['tramp',240,420,0.15]]                                         // 4 トランポリン 6.5s
4: [['rail',468,679,0.18],['rail',372,587,0.57],['belt',272,322,0.22]] // 5 ベルコン   3.0s
5: [['seesaw',310,400,0]]                                           // 6 シーソー     7.0s
6: [['plate',205,406,-0.13],['plate',191,665,0.86]]                 // 7 ワープ       6.9s
7: [['rail',219,269,0.8]]                                           // 8 スイッチ     5.1s
8: [['plate',219,269,0.8]]                                          // 9 ふりこ       4.0s
9: [['tramp',254,370,0.4]]                                          // 10 総合        5.4s
10:[['rail',177,371,0.84],['plate',236,403,0.64],['rail',126,345,-0.25]]  // 11 ジグザグ   4.3s
11:[['domino',438,223,0.04]]                                        // 12 ドミノ橋    5.1s
12:[['fan',177,371,0.84],['plate',236,403,0.64],['fan',126,345,-0.25]]    // 13 風で伸ばす 4.1s
13:[['jump',166,249,0.33],['jump',180,326,-0.16]]                   // 14 二段ジャンプ 3.3s
14:[['rail',278,651,0.22],['rail',329,517,-0.2],['plate',348,327,0.59]]   // 15 渦まで運ぶ 4.5s
15:[['rail',240,230,0.15]]                                          // 16 行って戻る  8.8s
16:[['rail',342,520,0.49],['rail',278,197,0.07]]                    // 17 積み上げ    5.1s
17:[['rail',278,651,0.22],['rail',329,517,-0.2],['plate',348,327,0.59]]   // 18 逆向き    3.9s
18:[['plate',219,269,0.8]]                                          // 19 ふりこで送る 4.0s
19:[['plate',177,371,0.84],['tramp',236,403,0.64],['rail',126,345,-0.25]] // 20 大回り    3.2s
```

### 20本に増やしたときの効率メモ

ステージ量産で時間を食ったのは «0クリア» になったステージのデバッグだった。
原因は毎回2つのどちらかで、どちらも**素の落下経路を1回出力すれば即分かる**。

1. **玉が想定の場所を通っていない** — 出発台の傾きで着地点が数十px ずれ、
   棚の端や溝に落ちていた。
2. **玉が途中で «止まって» いる** — 平らな棚の端で壁に寄りかかる、
   受け皿とレールのV字に挟まる、など。棚をわずかに傾けると直る。

なので、新ステージは**まず部品ゼロで走らせて経路を1本出す**。
これを最初にやらなかったステージだけ、何度も往復した。

---

## 6. 実機に出す：GitHub Pages を選んだ理由

Vercel と迷ったが、**手が止まらない方**を採った。

| | GitHub Pages | Vercel |
|---|---|---|
| 必要な手作業 | ゼロ（`gh` が認証済みなので repo作成〜push〜Pages有効化まで一気に通る） | vercel.com でのimport操作かCLIログインが要る |
| 反映 | push → Actions → 約30秒 | push → 約30秒 |

`vite.config.js` の **`base: './'`** が要点。相対パスで吐くので
Pages のサブパス（`/yokobo-pitagora/`）でも Vercel のルートでもそのまま動く。
どちらに移しても壊れないので、後から乗り換えても構わない。

配信URL: https://yokobo103.github.io/yokobo-pitagora/

## 7. 指で遊べるようにするために変えた所

PC用に作ったUIは、そのままでは実機で使えなかった。直したのは3点。

1. **回転をツールバーから外し、パーツ本体に付けた** — 選択中のパーツに青い丸（回転）と
   赤い×（削除）が出る。回転は**掴んだ角度を基準にした相対回転**にした。
   絶対角だと、ハンドルに触れた瞬間にパーツが「指の方向」へ飛ぶ。
2. **横持ち前提のレイアウト** — コースが 960×620 の横長なので、縦持ちだと
   キャンバスが 343×222 まで縮む。横持ちでは画面いっぱいに使い、
   パレットは右の 104px 列にアイコンだけで並べる。縦持ちには回転を促す案内を出す。
3. **ブラウザのジェスチャ殺し** — `touch-action: none`（キャンバス）、
   `overscroll-behavior: none`、`user-scalable=no`。これが無いと
   ドラッグでページが動き、ダブルタップでズームする。

## 8. 実機で触って分かったこと（ここが本題）

所長が実機で遊んだ結果、返ってきた指摘は2つ。**どちらも机上では出なかった**。

### (1) 「スマホの横持ちはブラウザゲームだと遊びづらい」

横持ち前提はそもそも間違いだった。レイアウトを縦にするだけでは足りず、
**盤面そのものを 960×620 → 540×810 の縦長に作り直し、4ステージ全部を組み直した**。

盤面を «狭く» したのが効いた。スケールが 0.58 → 0.66 に上がり、
同じ画面幅でもパーツが大きく描かれて掴みやすくなる。
横長のまま縦画面に押し込むと 343×222 まで縮んで話にならない。

物理定数（重力・パーツ寸法・ドミノ補助）は一切触っていないので、
壊れたのはステージの数字だけ。**«盤面の形» と «物理» を分けておいた設計が効いた。**

### (2) 「回転がさせづらい」

盤面に置いた小さい丸をドラッグする方式は、マウスでは快適でも指では駄目だった。
半径17（画面上10px程度）は指より小さく、しかも**掴んだ指がパーツを隠す**。

やめて、**選択中パーツ専用の操作卓**にした。パーツをタップすると
パレットがそのまま «かたむきスライダー ＋ ↺↻ ＋ 🗑» に差し替わる。

- 操作は盤面の外にあるので、指が見たい物を隠さない
- スライダーは48px高、ボタンは52×48。指の的として十分
- 盤面上のハンドルは `(pointer: fine)` の環境だけに残した（マウスなら直接操作の方が速い）

差し替え方式で気付いた副作用: **置いた直後に自動選択すると、パレットが消えて
連続配置ができなくなる**。ドミノを7本並べるステージで詰まる。
「置く」では選択せず、「タップする」で初めて選択、に変えて解消した。

> 未確認: `100dvh` の細かい挙動やマルチタッチは実機で見る必要がある。
> レイアウトの数値検証はビューポート擬似（375×812 / 1280×860）で行った。

## 9. 拡大の閉じ込め（実機で «戻せなくなる» 事故）

所長の報告: 「パーツを動かそうとすると画面がアップになって戻せなくなる」。

**iOS Safari は viewport の `user-scalable=no` を無視する**（iOS 10 以降）。
`touch-action: none` だけでは、ドラッグ中に2本目の指が触れたときのピンチや
素早い2連続タップの拡大を防げない。JS で塞ぐ必要がある。

```js
['gesturestart','gesturechange','gestureend'].forEach(t =>
  document.addEventListener(t, e => e.preventDefault(), {passive:false}));
document.addEventListener('touchstart', e => {
  if (e.touches.length > 1) e.preventDefault();
}, {passive:false});
```

2連続タップの抑止では **ボタンとスライダーを除外する**のが要点。
除外しないと ↻ ボタンの連打が効かなくなる（合成clickが消えるため）。

それでも拡大された場合の逃げ道として、`visualViewport.scale > 1` を検知して
「表示をもどす」ボタンを出す。押すと viewport meta を一瞬差し替えて戻す。
**戻せないのが一番困る**ので、防止と脱出の両方を用意する。

## 10. ギミックの増やし方

10ステージに増やすにあたって、仕掛けを2種類に分けた。

**置けるパーツ**（プレイヤーが操作する）: トランポリン・ふりこを追加。
**ステージ側の仕掛け**（盤面に最初からある）: ワープ・スイッチ／とびらを追加。

後者を «パーツにしない» のが効いた。ワープは入口と出口の対応付けが要るし、
スイッチはどのとびらを開けるかの紐付けが要る。エディタでこれをやらせると
UIが一気に複雑になる。**ステージデータに書くだけなら数行で済み、
しかもステージ設計の幅は同じだけ広がる。**

### パーツが «必要» になる条件は、物理的にそれしかできないこと

新パーツを入れても、板とレールで解けてしまうステージが多発した。
総当たりで確認すると、トランポリン用に作ったステージが板1枚で解けていた。

区別がついたのは、**«坂では絶対にできないこと» を課題にした**とき。

| パーツ | 坂で代用できない理由 |
|---|---|
| トランポリン | 落ちた高さより上へは行けない。跳ね上げだけが高さを増やせる |
| ワープ | 天井から床まで塞げば、通り抜ける手段が他にない |
| スイッチ | とびらを壁の端から端まで届かせれば、迂回路が消える |
| ドミノ | 玉自身が行けない場所へ «力» だけを送れる |

逆にベルコン・シーソー・ふりこは、うまく置いた板でも代用できてしまう。
これらは «必須» にはせず、**明らかに楽な答え**として置く割り切りにした。

### 抜け道は総当たりで探す

ランダム配置200通りを流して «クリアしてしまう組み合わせ» を集めると、
設計時に見えていなかった迂回路が出てくる。
「とびらの左に17px の隙間があって玉が通り抜ける」類は目視では絶対に見つからない。

## 11. collisionStart は «跳ね返った後» に呼ばれる

トランポリンが効かない原因がこれだった。matter-js の `collisionStart` は
ソルバが反発を解決した後に発火するので、その時点の `body.velocity` は
**衝突後**の速度。「入ってきた勢いを返す」実装がそのまま成立しない。

各ステップの頭で速度を控えておき、それを使う。

```js
stashVelocities() { /* Engine.update の前に body.preV へ退避 */ }
```

固定初速のジャンプ台では表面化しなかった。**入力速度に依存する仕掛けを
足したときに初めて出る罠**なので、覚えておく価値がある。

## 12. 5歳児が遊んで出た指摘（UIの本丸）

所長の子（5歳）が実際にハマって遊んだ結果の指摘は3つ。
**当初こちらが «問題だ» と思っていたものと、実際に困った点が違った**のが最大の学び。

| こちらの予想 | 実際 |
|---|---|
| スライダーは抽象的で5歳には無理 | **教えたら使いこなしていた**。作り直し不要、説明の問題 |
| 回転UIを作り直すべき | 困っていたのは «たまに掴めない» と «盤外に置くと消える» |

つまり **«分かりにくい» と «できない» を分けて聞く**必要があった。
分かりにくいものは説明で解ける。できないものだけ作り直す。

### 直したもの

1. **掴めない** — レールは画面上たった8px（判定込みでも18px、指は44px要る）。
   «その点を含むか» ではなく **«一番近いパーツを拾う»** に変更。20px外しても掴める。
   置く途中（パレット選択中）だけは判定を18に絞り、隣に置けなくなるのを防ぐ。
2. **盤外で消える** — ドラッグに範囲制限が無く、盤面の外へ持ち出せていた。
   データ上は残っているのに描画範囲外なので «消えた» ように見える。
   ドラッグ位置を盤面内にクランプし、読み込み時にも拾い直す。
3. **別のパーツを選ぶのに一旦解除が要る** — パレットと操作卓を差し替える
   モーダル設計が原因。**両方を常時表示**にした。操作卓は選択が無いときも
   場所を確保して説明文を出すので、盤面の大きさは変わらない（359×562を維持）。

### ついでに入れたもの

- **とりけし（Undo）** — 子どもは必ず誤操作する。戻れることが安心につながる
- **ぜんぶ消すは2段階**（1回目で「ほんとに?」）＋ Undo でも戻せる
- **初回に «あそびかた»** を表示。？ボタンでいつでも再表示

## 13. ブラウザペインが使えない環境用のヘッドレス検証

リモート実行だと preview を起動できず、それまでの検証手段（`__pita` をブラウザで叩く）が
丸ごと使えなくなった。**Puppeteer ＋ 素のhttpサーバでの自己完結スクリプト**に置き換えた。

```bash
node tools/verify.mjs --shot
```

出るのは4つ。これで «盤面の大きさ» と «指の操作» と «全20面が解けるか» が一度に分かる。

- 起動時のコンソールエラー
- レイアウト実測（canvas / side / ページのスクロール有無）
- 合成タッチの結果（22px外して掴めるか、盤外にドラッグしても残るか、Undoが効くか）
- 全20ステージの正解例クリア

**ペインが使えるかどうかに依存しない検証を持っておくと、環境が変わっても止まらない。**

## 14. ことばを «かな» に寄せる

5歳児が遊ぶので、画面に出る漢字を全部なくした。パーツ名・ステージ名・ヒント・
ボタン・結果画面・`title` 属性まで。

短くするときに効いた考え方:

- **形容詞つきの名前より «見た目の違い» を名前にする** — 「いた（短）／レール（長）」→
  「いた／ながいいた」。同じ物の大小だと分かる方が、別の名前より速い
- **「◯◯する」を「◯◯」に落とさない** — 「回転」ではなく「まわす」。
  子どもには名詞より動詞の方が通じる
- **数字と記号は残していい** — 「のこり 2」「45°」「3.7びょう」は読める

`tools/verify.mjs` に**漢字チェック**を入れた。全21ステージを順に表示して
テキストノードと `title` を走査するので、ステージを足したときの入れ忘れが止まる。

### ヒントの置き場所

スマホでは盤面の上にヒントを出す余白が無く、これまで非表示だった（＝子どもは目的が読めない）。
**選択中パーツの操作卓が空いているときにヒントを出す**ようにして解決した。
パーツを選ぶと操作に切り替わるので、場所を食い合わない。

## 15. ラボの紹介の置き方

「メインじゃなくていい」という要望だったので、**遊びの邪魔をしない2か所**に置いた。

- **あそびかた（？ボタン）の下** — 説明を読む気がある人だけが目にする
- **クリア画面の下に1行** — 一番気分がいい瞬間に、小さく

ゲーム本編（盤面・パレット・操作卓）には一切足していない。

## 16. さわって進むチュートリアル

「説明する場所が光って、実際に操作すると次へ進む」形にした。5歳児に**読ませない**のが狙い。

### 穴のあけ方

暗幕に穴を切り抜くのではなく、**光らせたい要素の上に枠を置いて
`box-shadow: 0 0 0 9999px rgba(...)` で外側を暗くする**。1要素で済み、
`pointer-events: none` にしておけば光った所はそのまま触れる。

```css
.coachhole { position: absolute; border: 3px solid var(--accent2);
             box-shadow: 0 0 0 9999px rgba(6,11,22,.74); }
```

位置は `getBoundingClientRect()` を毎ステップ取り直すだけ。`transition` を付けると
次のステップへ «滑って» 動くので、どこへ注目が移ったか目で追える。

### 盤面の中のものを光らせる

3歩目の「おいたパーツをタップ」だけは対象がDOMではなくキャンバスの中にある。
**ワールド座標 → 画面座標** に変換して矩形を作れば、同じ仕組みで光らせられる。

```js
const sx = cvRect.width / WORLD_W, sy = cvRect.height / WORLD_H;
// (p.x, p.y) と パーツ寸法を sx, sy で写すだけ
```

小さいパーツでも指で追えるよう、**最低70ワールド単位の枠**にしている。

### 進行はイベントで

タイマーや「つぎへ」ボタンではなく、**ゲーム側の実際の操作**で進める。
`coach.notify('pick' | 'place' | 'select' | 'rotate' | 'play')` を
パレット選択・設置・選択・角度変更・スタートの5か所から呼ぶだけ。
「言われた通りにやったら進んだ」という手応えが出るのが大きい。

`tools/verify.mjs` で7ステップを合成タッチで通し、
最後に自動で閉じるところまで確認している（ステップを足しても壊れたら止まる）。

### 枠で囲むだけでは «光った» と分からない

所長の指摘: 「スタートが光るのが分かりにくい」。原因は
**▶スタートが元から明るい黄色**で、まわりが暗くなっても «変化» として読めないこと。
暗い要素なら暗幕との差で目立つが、明るい要素には効かない。

足したのは3つ。**囲むのではなく «動かす» のが決め手**だった。

1. 光っている当の要素自体を脈打たせる（`scale(1) ⇄ 1.055`）
2. 小さい対象（幅220px未満）には指マーク 👆 を添えて上下に跳ねさせる
3. 暗幕を濃く（.74→.82）、枠を太く（3→4px）

盤面のような大きい枠には指を出さない。枠が十分大きければそれ自体が目印になる。

### «さわる所» と «変わる所» は別

所長の指摘: 「盤面が暗くなるので、かたむきスライダーの説明のとき
どこが変化してるか分かりにくい」。

スライダーを光らせると盤面は暗いままなので、**動かした結果が見えない**。
操作の説明には **原因（さわる所）と結果（変わる所）の2か所**が要る。

そのために暗幕を `box-shadow` から **SVGマスク**に変えた。
box-shadow の穴は1つしか開かないが、マスクなら好きな数だけ開く。

```html
<mask id="coachHoles">
  <rect width="100%" height="100%" fill="#fff"/>
  <rect id="holeA" fill="#000"/>   <!-- さわる所 -->
  <rect id="holeB" fill="#000"/>   <!-- 変わる所 -->
</mask>
```

枠の色で役割を分けた。**水色＝さわる所（脈打つ＋指）**、
**黄色の破線＝変わる所（「みてて」の札）**。

### 連続イベントで «進みすぎる»

所長の指摘: 「回転させたらすぐ『もどす』の説明になる。スタートの説明にならない」。

原因は **`input` が連続で飛ぶこと**。スライダーを1回動かすだけで数十回発火し、
その回数だけ `setTimeout(next, 260)` が予約されて一気に最後まで進んでいた。

```js
notify(ev) {
  if (!this.active || this.pending) return;   // 予約は常に1つだけ
  ...
  this.pending = true;
  setTimeout(() => { this.pending = false; this.next(); }, 260);
}
```

待ち時間つきのステップにも同じ穴があった。待っている間にユーザーが先へ進むと、
タイマーが後から古いステップを表示してしまう。**待ち始めたときの番号を控え、
まだそこに居るときだけ表示する**ようにした。

> 検証が見逃した理由がはっきりしている。テストで `input` を**1回しか撃っていなかった**。
> **連続で飛ぶイベントは、テストでも連続で撃つ。**
> 直したあと、ガードを外すとテストが落ちることまで確認した
> （落ちないテストは、通っても何も保証しない）。

## 17. キャラをどこから持ってくるか

Dr.よこぼ と ニャビットを出すにあたって、素材の出どころが2つあった。

| 出どころ | 中身 | この用途での可否 |
|---|---|---|
| キャスティング `use.py <id> web` | `cutout.png`（正本の姿・写実寄り・表情1つ） | 使えるが**表情が1つ**しかない |
| CM動画作成パイプライン `assets/characters/` | **ステッカー調・白フチつき・表情4種**（透過済み） | **こちらを採用** |

決め手は2つ。**白フチ**があるので暗い画面の上でも輪郭が消えない。
そして**表情がある**ので、結果を文字でなく顔で伝えられる。

- ラボ紹介: よこぼ `idea`（ひらめき）＋ ニャビット `happy`
- クリア: ニャビット `happy`
- とどかなかった: ニャビット `surprise`（「おっと」の顔。`warning` は叱る感じになるので使わない）

`tools/make_chara.py` で透明余白を落として高さ400pxのWebPにする。各28〜31KB。
**素材を直接コピーせず変換スクリプトを置く**ことで、元が差し替わったら流し直せる。

> キャスティングの `use.py` は «正本はどれか» を答える装置で、
> «この用途にどの絵が向くか» は別の判断。両方見てから選ぶのが正しかった。

## 18. 音は «録らずに合成する»

音源ファイルを貼らず、WebAudioでその場で合成した。理由は容量ではなく**手ざわり**。

物理ゲームでは **«ぶつかった速さ» で音が変わること**が気持ちよさの大半を占める。
同じ音を鳴らすと、ドミノが7本倒れる間に同じ「カチッ」が7回鳴って一気に安っぽくなる。
合成なら衝突速度をそのまま音の高さと大きさに写せる。

| 音 | 作り |
|---|---|
| ぶつかる | バンドパスしたノイズ + 低いトライアングル。速さで周波数と音量が動く |
| ドミノ | 上より低く木質に寄せた別系統（`label === 'domino'` で分岐） |
| ジャンプ／トランポリン | 180Hz→620Hz のスイープ。トランポリンは出た速度で行き先が変わる |
| ワープ | 上昇スイープ + ハイパスノイズ |
| スイッチ | 短い矩形波2つ |
| クリア | ドミソド のアルペジオ（ぜんぶクリアは1音多い） |

副産物としてビルドは **+1.2KB gzip** しか増えていない（音源0バイト）。

**罠2つ。**
- ブラウザの自動再生制限で、`AudioContext` は**最初のタップの中でしか作れない**。
  `window.addEventListener('pointerdown', initAudio, { once: true })` で起こす。
  検証では `--autoplay-policy=no-user-gesture-required` を付け、
  **`ctx.state` が `running` になっているか**まで見る（`suspended` のまま無音、が一番怖い）。
- **ぶつかる音は間引く。** 毎フレーム衝突が起きるので、45ms以内の連発は捨てる。

BGMは別で、こちらは合成では作れない。作るなら `sound` スキル（ACE-Step）の出番。

## 次にやるなら

1. **音** — 転がる音・ドミノのカチカチ・クリアのファンファーレ。この手のゲームは音で化ける（`sound` スキルが使える）
2. **リプレイ／共有** — 配置データが正本なので、URLに載せれば「作ったコース」を配れる。ここが本家の肝
3. **パーツ追加** — バネ、ワープ、分岐レバー、ボタンで動く扉
4. **スマホ実機** — pointer events で組んであるので動くはずだが、パレットの押しやすさは未検証
5. **ステージエディタ** — 固定物も置けるようにすれば、そのまま作品投稿型になる
